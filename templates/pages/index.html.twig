{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        body {
            background-color: #1c1c1c;
            color: #ffffff;
        }

        h1, h2 {
            color: #fff8e1; /* Blanc cassé pour les titres */
            font-weight: bold;
        }

        p {
            color: #f5f5f5; /* Texte clair */
        }

        .hero-img {
            max-width: 100%;
            height: auto;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(255, 255, 255, 0.2);
            margin-bottom: 30px;
        }

        .btn-ija {
            background-color: #ff9800; /* Orange vif */
            color: white;
            font-size: 1.2rem;
            padding: 12px 24px;
            border-radius: 8px;
            transition: background-color 0.3s, transform 0.2s;
        }

        .btn-ija:hover {
            background-color: #f57c00;
            transform: scale(1.05);
            text-decoration: none;
        }

        .def-block {
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 20px;
        }

        .def-1 {
            background-color: rgba(255, 152, 0, 0.85); /* Orange clair */
            color: #fff;
        }

        .def-2 {
            background-color: rgba(76, 175, 80, 0.85); /* Vert doux */
            color: #fff;
        }

        .def-3 {
            background-color: rgba(255, 235, 59, 0.85); /* Jaune doux */
            color: #212121;
        }

        hr {
            border-color: #ffffff;
        }
    </style>
{% endblock %}

{% block title %}Kech/Moto{% endblock %}

{% block body %}
    <div class="container text-center py-5">
        <h1>Découvrez les meilleures motos du marché ! chez <strong>KECH/MOTO</strong></h1>
         


        <div class="mt-4">
            <a href="{{ path('app_produit') }}" class="btn btn-ija">
                Voir les choix
            </a>
        </div>

        <hr class="my-5">

        <div>
        
 </div>
       Bienvenue sur KECH/Moto, votre site spécialisé dans la vente de motos puissantes et stylées. Découvrez une sélection de modèles haut de gamme, avec une interface moderne et immersive inspirée de l’univers biker. Que vous soyez passionné ou simple amateur, trouvez la moto de vos rêves en quelques clics. 🏍️🔥💨

    </div>
{% endblock %}
